import React from 'react'

const FixedHeader = () => { 

    return (
        <div style={{position: 'fixed', top: '0.5rem', right: '1rem'}} className="text-muted">
            @fauzanazhima from <span className="text-dark">Unsplash</span> 
        </div>
    )
}

export default FixedHeader